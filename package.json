{
  "name": "zero-collateral-subgraph",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ zero-collateral/zero-collateral",
    "create-local": "graph create --node http://127.0.0.1:8020/ zero-collateral/zero-collateral",
    "create-docker": "graph create --node http://$GRAPH_HOST:$GRAPH_PORT/ zero-collateral/zero-collateral",
    "remove-local": "graph remove --node http://127.0.0.1:8020/ zero-collateral/zero-collateral",
    "remove-docker": "graph remove --node http://$GRAPH_HOST:$GRAPH_PORT/ zero-collateral/zero-collateral",
    "deploy-local": "graph deploy --node http://127.0.0.1:8020/ --ipfs http://localhost:5001 zero-collateral/zero-collateral",
    "deploy-docker": "graph deploy --node http://$GRAPH_HOST:$GRAPH_PORT/ --ipfs http://$IPFS_HOST:$IPFS_PORT zero-collateral/zero-collateral",
    "prepare:ganache": "mustache config/ganache.json subgraph.template.yaml > subgraph.yaml",
    "prepare:ropsten": "mustache config/ropsten.json subgraph.template.yaml > subgraph.yaml",
    "deploy:ganache": "yarn remove-local; yarn prepare:ganache && yarn create-local && yarn build && yarn deploy-local",
    "deploy:ropsten": "yarn prepare:ropsten && yarn build && yarn deploy",
    "deploy:docker": "yarn remove-docker; yarn prepare:ganache && yarn create-docker && yarn deploy-docker"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.18.0",
    "@graphprotocol/graph-ts": "0.18.1",
    "mustache": "^4.0.1"
  }
}
